import { StyleSheet } from 'react-native'

export const titleStyles = StyleSheet.create({
  container: {
    fontSize: 24,
    paddingBottom: 40,
    fontWeight: '300',
    fontFamily: 'Sono',
  },
})
